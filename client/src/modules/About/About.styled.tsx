import styled from "styled-components";

const Text = styled.div`
  width: 50vw;
  height: fit-content;
  padding: 2em 0;
  margin: 0 auto;
  color: var(--yellow);
  line-break: strict;
`;
export const S = { Text };
