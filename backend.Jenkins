pipeline{
    agent any
    stages {
        stage("cli") {
            
            steps {
                withCredentials([usernamePassword(credentialsId: 'tripapps-ssh',usernameVariable: "USER",passwordVariable:"PASS")]) {
                    echo "${USER} ${PASS}"
                }
            }
        }
    }
}